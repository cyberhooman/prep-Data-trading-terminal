# Application Environment
NODE_ENV=development
PORT=3000

# Database (PostgreSQL for production, auto-configured by Railway)
DATABASE_URL=postgresql://user:password@host:port/database

# Google OAuth (for authentication)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Session Secret (generate a random string)
SESSION_SECRET=your_random_session_secret_here

# DeepSeek AI (for central bank speech analysis)
# Get your API key from https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Email Service (Gmail SMTP for password reset emails)
# 1. Go to your Google Account: https://myaccount.google.com/
# 2. Navigate to Security → 2-Step Verification (must be enabled first)
# 3. Scroll down to "App passwords" and click it
# 4. Select "Mail" and "Other (Custom name)" → Name it "Alphalabs Trading"
# 5. Click "Generate" and copy the 16-character password
# 6. Use that password below (remove spaces)
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password-here

# Application URL (for password reset emails)
APP_URL=http://localhost:3000

# Production Settings (Railway will set these automatically)
# NODE_ENV=production
# DATABASE_URL will be auto-injected by Railway when you add PostgreSQL
# Update GOOGLE_CALLBACK_URL to your Railway domain: https://your-app.railway.app/auth/google/callback
# Update APP_URL to your Railway domain for password reset emails
